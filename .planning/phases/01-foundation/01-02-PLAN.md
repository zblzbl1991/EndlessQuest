---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/uiCopy.ts
  - src/pages/BuildingsPage.tsx
  - src/pages/CharactersPage.tsx
  - src/pages/VaultPage.tsx
  - src/components/common/CharacterCard.tsx
  - src/components/building/AlchemyPanel.tsx
  - src/components/building/ForgePanel.tsx
  - src/components/building/MarketPanel.tsx
  - src/stores/sectStore/characterSlice.ts
  - src/stores/sectStore/buildingSlice.ts
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "Quality label text (凡品/灵品/仙品/神品/混沌) is defined in exactly one source file"
    - "Quality order array is defined in exactly one source file"
    - "All 7 consumer files import quality labels from uiCopy.ts instead of defining them locally"
  artifacts:
    - path: "src/data/uiCopy.ts"
      provides: "Centralized CHAR_QUALITY_NAMES and CHAR_QUALITY_ORDER exports"
      exports: ["CHAR_QUALITY_NAMES", "CHAR_QUALITY_ORDER"]
    - path: "src/pages/BuildingsPage.tsx"
      provides: "Imports CHAR_QUALITY_NAMES from uiCopy, no local QUALITY_LABELS"
    - path: "src/pages/CharactersPage.tsx"
      provides: "Imports CHAR_QUALITY_NAMES from uiCopy, no local QUALITY_NAMES_CHAR"
    - path: "src/components/common/CharacterCard.tsx"
      provides: "Imports CHAR_QUALITY_NAMES from uiCopy, no local QUALITY_NAMES"
  key_links:
    - from: "src/pages/BuildingsPage.tsx"
      to: "src/data/uiCopy.ts"
      via: "import { CHAR_QUALITY_NAMES }"
      pattern: "import.*CHAR_QUALITY_NAMES.*uiCopy"
    - from: "src/pages/CharactersPage.tsx"
      to: "src/data/uiCopy.ts"
      via: "import { CHAR_QUALITY_NAMES }"
      pattern: "import.*CHAR_QUALITY_NAMES.*uiCopy"
    - from: "src/components/common/CharacterCard.tsx"
      to: "src/data/uiCopy.ts"
      via: "import { CHAR_QUALITY_NAMES }"
      pattern: "import.*CHAR_QUALITY_NAMES.*uiCopy"
---

<objective>
Consolidate all character quality label and quality order definitions into a single source file.

Purpose: Character quality labels (凡品/灵品/仙品/神品/混沌) and quality ordering arrays are currently defined in 7+ separate locations. This duplication creates maintenance risk -- any label change requires editing all locations. Consolidating into uiCopy.ts creates a single source of truth.
Output: All character quality labels imported from src/data/uiCopy.ts; all local definitions removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- The canonical quality type from character.ts -->
From src/types/character.ts (line 9):
```typescript
export type CharacterQuality = 'common' | 'spirit' | 'immortal' | 'divine' | 'chaos'
```

<!-- The existing QUALITY_NAMES in items.ts is for ItemQuality, NOT CharacterQuality -->
From src/data/items.ts (lines 3-11):
```typescript
// NOTE: This is ItemQuality, not CharacterQuality. Items.ts QUALITY_NAMES stays as-is.
export const QUALITY_ORDER: ItemQuality[] = ['common', 'spirit', 'immortal', 'divine', 'chaos']
export const QUALITY_NAMES: Record<ItemQuality, string> = {
  common: '凡品', spirit: '灵品', immortal: '仙品', divine: '神品', chaos: '混沌',
}
```

<!-- The 7 locations to update (per CONTEXT.md D-02): -->

1. src/pages/BuildingsPage.tsx line 63:
   `const QUALITY_LABELS: Record<CharacterQuality, string> = { common: '凡', ... }`
   -- NOTE: BuildingsPage uses single-char labels ('凡', '灵', '仙', '神', '混沌') not full names. Check actual values before changing.

2. src/pages/CharactersPage.tsx line 43:
   `const QUALITY_NAMES_CHAR: Record<CharacterQuality, string> = { ... }`

3. src/components/common/CharacterCard.tsx line 24:
   `const QUALITY_NAMES: Record<CharacterQuality, string> = { ... }`
   Also exports it at line 159.

4. src/components/building/AlchemyPanel.tsx line 6:
   `const QUALITY_LABELS: Record<string, string> = { ... }`

5. src/components/building/ForgePanel.tsx line 8:
   `const QUALITY_LABELS: Record<string, string> = { ... }`

6. src/components/building/MarketPanel.tsx line 8:
   `const QUALITY_LABELS: Record<string, string> = { ... }`

7. src/stores/sectStore/characterSlice.ts line 43:
   `const qualityLabel: Record<string, string> = { ... }`

Plus quality order arrays:
- src/stores/sectStore/characterSlice.ts line 222: `const QUALITY_ORDER: CharacterQuality[]`
- src/stores/sectStore/buildingSlice.ts line 12: `const QUALITY_ORDER: Character['quality'][]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centralized quality constants to uiCopy.ts</name>
  <files>src/data/uiCopy.ts</files>
  <read_first>
    src/data/uiCopy.ts (current contents)
    src/pages/BuildingsPage.tsx (lines 60-75 for QUALITY_LABELS definition)
    src/pages/CharactersPage.tsx (lines 40-52 for QUALITY_NAMES_CHAR definition)
    src/components/common/CharacterCard.tsx (lines 22-35 for QUALITY_NAMES definition)
    src/components/building/AlchemyPanel.tsx (lines 4-15 for QUALITY_LABELS)
    src/components/building/ForgePanel.tsx (lines 6-18 for QUALITY_LABELS)
    src/components/building/MarketPanel.tsx (lines 6-18 for QUALITY_LABELS)
    src/stores/sectStore/characterSlice.ts (lines 40-55 for qualityLabel, lines 220-225 for QUALITY_ORDER)
    src/stores/sectStore/buildingSlice.ts (lines 10-18 for QUALITY_ORDER)
  </read_first>
  <action>
    Per D-02 from CONTEXT.md:

    **IMPORTANT**: Before making any changes, read each file and note the EXACT label values used. Some files may use abbreviated labels (e.g., '凡' vs '凡品'). The centralized constant must serve all consumers.

    **Step 1: Add to src/data/uiCopy.ts**

    Add the following exports at the end of the file:

    ```typescript
    import type { CharacterQuality } from '../types/character'

    /** Character quality display names (Chinese labels). Single source of truth. */
    export const CHAR_QUALITY_NAMES: Record<CharacterQuality, string> = {
      common: '凡品',
      spirit: '灵品',
      immortal: '仙品',
      divine: '神品',
      chaos: '混沌',
    }

    /** Character quality ordering from lowest to highest. */
    export const CHAR_QUALITY_ORDER: CharacterQuality[] = ['common', 'spirit', 'immortal', 'divine', 'chaos']
    ```

    Use the full form labels (凡品, 灵品, etc.) since they are the most commonly used. If any consumer uses abbreviated forms, it can do a `.replace('品', '')` or we add a separate constant -- but first READ the actual values in each file to check.
  </action>
  <acceptance_criteria>
    - `grep -c "CHAR_QUALITY_NAMES" src/data/uiCopy.ts` returns >= 1
    - `grep -c "CHAR_QUALITY_ORDER" src/data/uiCopy.ts` returns >= 1
    - `grep "import type.*CharacterQuality" src/data/uiCopy.ts` returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Project/endlessQuest && grep -c "CHAR_QUALITY_NAMES" src/data/uiCopy.ts && grep -c "CHAR_QUALITY_ORDER" src/data/uiCopy.ts</automated>
  </verify>
  <done>uiCopy.ts exports CHAR_QUALITY_NAMES (Record&lt;CharacterQuality, string&gt;) and CHAR_QUALITY_ORDER (CharacterQuality[]).</done>
</task>

<task type="auto">
  <name>Task 2: Replace all local quality definitions with imports from uiCopy.ts</name>
  <files>src/pages/BuildingsPage.tsx, src/pages/CharactersPage.tsx, src/pages/VaultPage.tsx, src/components/common/CharacterCard.tsx, src/components/building/AlchemyPanel.tsx, src/components/building/ForgePanel.tsx, src/components/building/MarketPanel.tsx, src/stores/sectStore/characterSlice.ts, src/stores/sectStore/buildingSlice.ts</files>
  <read_first>
    src/data/uiCopy.ts (to see the new exports from Task 1)
    All 9 files listed above (to see current local definitions and usage patterns)
    src/data/items.ts (lines 1-11 -- confirm this is ItemQuality, NOT CharacterQuality -- do NOT change items.ts)
    src/components/common/CharacterCard.tsx (line 159 exports QUALITY_NAMES -- check if any other file imports from here)
  </read_first>
  <action>
    Per D-02 from CONTEXT.md. For each of the 9 files, replace local quality definitions with imports from uiCopy.ts.

    **IMPORTANT PRE-CHECK**: Run `grep -rn "from.*CharacterCard.*QUALITY_NAMES" src/` to check if CharacterCard's exported QUALITY_NAMES is imported anywhere else. If so, those imports must also be updated.

    **For each file, the pattern is:**
    1. Add `import { CHAR_QUALITY_NAMES } from '../data/uiCopy'` (adjust relative path per file location)
    2. Add `import { CHAR_QUALITY_ORDER } from '../data/uiCopy'` if the file also has a local QUALITY_ORDER
    3. Add `import type { CharacterQuality } from '../types/character'` if needed (for type annotation)
    4. Remove the local constant definition (QUALITY_LABELS, QUALITY_NAMES_CHAR, qualityLabel, etc.)
    5. Replace all usage sites of the old name with `CHAR_QUALITY_NAMES`

    **File-by-file details:**

    **src/pages/BuildingsPage.tsx:**
    - Remove local `QUALITY_LABELS` at line 63
    - Import `CHAR_QUALITY_NAMES` from `'../data/uiCopy'`
    - Replace `QUALITY_LABELS` with `CHAR_QUALITY_NAMES` at lines 588, 603, 623, 634, 676

    **src/pages/CharactersPage.tsx:**
    - Remove local `QUALITY_NAMES_CHAR` at line 43
    - Import `CHAR_QUALITY_NAMES` from `'../data/uiCopy'`
    - Replace `QUALITY_NAMES_CHAR` with `CHAR_QUALITY_NAMES` at line 406

    **src/pages/VaultPage.tsx:**
    - Currently imports `QUALITY_NAMES` from `'../data/items'` -- this is ItemQuality QUALITY_NAMES which happens to have the same values. Per D-02, replace with `CHAR_QUALITY_NAMES` from uiCopy to use the CharacterQuality-typed version for consistency. BUT only if VaultPage uses it for character qualities. READ the usage context first -- if VaultPage shows item quality labels, keep the items.ts import and do NOT change it.

    **src/components/common/CharacterCard.tsx:**
    - Remove local `QUALITY_NAMES` at line 24 and the re-export at line 159
    - Import `CHAR_QUALITY_NAMES` from `'../../data/uiCopy'`
    - If other files import `QUALITY_NAMES` from CharacterCard, update those imports to use uiCopy instead

    **src/components/building/AlchemyPanel.tsx:**
    - Remove local `QUALITY_LABELS` at line 6
    - Import `CHAR_QUALITY_NAMES` from `'../../data/uiCopy'`
    - Replace `QUALITY_LABELS` with `CHAR_QUALITY_NAMES` at lines 56

    **src/components/building/ForgePanel.tsx:**
    - Remove local `QUALITY_LABELS` at line 8
    - Import `CHAR_QUALITY_NAMES` from `'../../data/uiCopy'`
    - Replace `QUALITY_LABELS` with `CHAR_QUALITY_NAMES` at line 68

    **src/components/building/MarketPanel.tsx:**
    - Remove local `QUALITY_LABELS` at line 8
    - Import `CHAR_QUALITY_NAMES` from `'../../data/uiCopy'`
    - Replace `QUALITY_LABELS` with `CHAR_QUALITY_NAMES` at lines 138, 179

    **src/stores/sectStore/characterSlice.ts:**
    - Remove local `qualityLabel` at line 43
    - Import `CHAR_QUALITY_NAMES` from `'../../data/uiCopy'`
    - Replace `qualityLabel` with `CHAR_QUALITY_NAMES` at line 50
    - Remove local `QUALITY_ORDER` at line 222
    - Import `CHAR_QUALITY_ORDER` from `'../../data/uiCopy'`
    - Replace `QUALITY_ORDER` with `CHAR_QUALITY_ORDER` at lines 223, 241

    **src/stores/sectStore/buildingSlice.ts:**
    - Remove local `QUALITY_ORDER` at line 12
    - Import `CHAR_QUALITY_ORDER` from `'../../data/uiCopy'`
    - Replace `QUALITY_ORDER` with `CHAR_QUALITY_ORDER` at line 15

    **DO NOT CHANGE:**
    - src/data/items.ts -- its QUALITY_NAMES and QUALITY_ORDER are for ItemQuality, not CharacterQuality
    - src/systems/pet/PetSystem.ts -- PET_QUALITY_NAMES uses PetQuality type
    - src/systems/item/ItemGenerator.ts -- uses ItemQuality QUALITY_NAMES from items.ts
    - src/components/inventory/ItemCard.tsx -- uses ItemQuality QUALITY_NAMES from items.ts

    After all changes, run `npx vitest run` to verify no regressions.
  </action>
  <acceptance_criteria>
    - `grep -rn "const QUALITY_LABELS" src/pages/ src/components/` returns 0 matches
    - `grep -rn "const QUALITY_NAMES_CHAR" src/pages/` returns 0 matches
    - `grep -rn "const qualityLabel" src/stores/` returns 0 matches
    - `grep -rn "const QUALITY_NAMES.*Record.*CharacterQuality" src/components/common/CharacterCard.tsx` returns 0 matches
    - `grep -rn "const QUALITY_ORDER.*CharacterQuality\|const QUALITY_ORDER.*Character\['quality'\]" src/stores/` returns 0 matches
    - `grep -rn "import.*CHAR_QUALITY_NAMES.*uiCopy" src/` returns >= 7 matches (one per consumer)
    - `grep -rn "import.*CHAR_QUALITY_ORDER.*uiCopy" src/` returns >= 2 matches (characterSlice + buildingSlice)
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Project/endlessQuest && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 consumer files import CHAR_QUALITY_NAMES from uiCopy.ts. Both slice files import CHAR_QUALITY_ORDER from uiCopy.ts. Zero local quality label/order definitions remain in consumers. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cd C:/Project/endlessQuest && npx vitest run` -- all tests pass
2. `grep -rn "const QUALITY_LABELS\|const QUALITY_NAMES_CHAR\|const qualityLabel" src/pages/ src/components/ src/stores/` -- 0 matches (local defs removed)
3. `grep -rn "CHAR_QUALITY_NAMES" src/ | wc -l` -- >= 10 (1 definition + >= 7 imports + >= 2 usages)
4. `npm run build` -- build succeeds with no type errors
</verification>

<success_criteria>
- CHAR_QUALITY_NAMES defined once in uiCopy.ts, imported by all 7 consumer files
- CHAR_QUALITY_ORDER defined once in uiCopy.ts, imported by characterSlice and buildingSlice
- Zero local quality label/order constant definitions remain in page/component/slice files
- items.ts QUALITY_NAMES (for ItemQuality) is unchanged
- All existing tests pass, TypeScript build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
